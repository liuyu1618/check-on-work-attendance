<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.project.mapper.TeacherMapper">
    <insert id="insert">
        insert into teacher(t_name, t_password, t_phone, t_gender)
        values (#{tName}, #{tPassword}, #{tPhone}, #{tGender})
    </insert>

    <delete id="delete">
        delete from teacher where t_id =#{tId}
    </delete>

    <update id="update">
        update teacher set t_name=#{tName},t_password=#{tPassword}
                           ,t_phone=#{tPhone},t_gender=#{tGender}
            where t_id=#{tId}
    </update>
</mapper>